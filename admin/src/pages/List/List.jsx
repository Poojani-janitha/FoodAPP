import React from 'react'
import './List.css'

const list = () => {
  return (
    <div>
      const [list, setList] = useState([]);
      const fetchList = async () => {
        const response awa
    </div>
  )
}

export default list
